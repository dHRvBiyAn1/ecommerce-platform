# Centralized config served to other services. Empty for now; per-service settings
# live alongside this file (auth-service.yml, order-service.yml, ...).

management:
  endpoints:
    web:
      exposure:
        include: health,info,prometheus

logging:
  level:
    com.project: INFO
    org.springframework.cloud.config: INFO
