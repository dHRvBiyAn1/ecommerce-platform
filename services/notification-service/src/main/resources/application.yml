spring:
  application:
    name: notification-service
  config:
    import: optional:configserver:http://config-server:8888
  mail:
    properties:
      mail.smtp.connectiontimeout: 25000
      mail.smtp.timeout: 25000
      mail.smtp.writetimeout: 25000
