spring:
  config:
    import: "optional:configserver:"
  cloud:
    config:
      enabled: false
  mongodb:
    uri: mongodb://root:root@localhost:27017/product_db?authSource=admin
  data:
    redis:
      host: localhost
      port: 6379
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.kafka.KafkaAutoConfiguration
